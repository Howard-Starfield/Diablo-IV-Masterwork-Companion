---
phase: 13-macro-v1-runtime-library
task: 13
total_tasks: 14
status: in_progress
last_updated: 2026-07-13T22:18:49.966Z
---

<current_state>
Macro V1 Task 13 is paused on branch `codex/macro-v1` in worktree `D:\AI_projects\Diablo_Masterwork_Companion\.worktrees\codex-macro-v1`. Product-code commits are clean through `8c1aca7`. Tasks 1-12 are accepted. The checked controller/lifecycle slice of Task 13 is accepted through `5bf1669`; package and live-core checkpoints are committed but still need review/remediation before UI composition.
</current_state>

<completed_work>

- Tasks 1-12: complete and independently reviewed through `b79e1dc`.
- Task 13 controller/lifecycle backend: commits `b1193b4`, `620514a`, and `5bf1669`; accepted after two cumulative review/remediation loops. It starts only checked durable revisions, supports Once/Continuous observation and dry-run extents, owns cancellation/drop safely, protects active revision/history deletion, and exposes bounded semantic monitor state.
- Task 13 package checkpoint: `546c045` adds checked export-current, bounded frozen package preparation, text commit, and a pending image re-verification transaction. Persistence tests passed 65/65; full suite passed 559/559. Independent review found three P1 gaps listed below, so this commit is not accepted yet.
- Task 13 live core checkpoint: `8c1aca7` adds `RunMode::Live`, detector routing, inline `ActionCommitter` execution, attempt-ID action transitions, fresh resume invalidation, and bounded Unlimited attempt retention. Runtime tests passed 111/111; monitor 14/14; full suite 559/559. Independent review was interrupted by the safe stop and must restart.
- All active subagents were stopped. Worktree was clean before this handoff file was added.
</completed_work>

<remaining_work>

- Remediate package P1: image import must bind a locally captured `TargetProfile` and locally confirmed/recaptured region geometry instead of saving portable target/region values.
- Remediate package P1: local image proof cannot be minted from caller-asserted PNG bytes, scores, hashes, or empty clusters. Use verifier-owned evidence from exact target-bound captures and actual negative image bytes/matching.
- Remediate package P1: replace metadata-then-`fs::read` and canonicalize-then-open with bounded handle-based reads (`take(max + 1)`) and no-follow/open-handle containment/identity checks to close growth and reparse/symlink races.
- Restart independent review of live commit `8c1aca7` (`546c045..8c1aca7`). Explicitly audit whether Pause is part of the commit-time `StopSource`, and whether public `with_live_session` can attach a no-op/unrelated `LiveControlSink` instead of runtime-owned control.
- After live core is accepted, add the separate Windows native factory: exact private HWND shared by WGC capture and `WindowsTargetGuard`, Windows OCR/image router, `WindowsInputSink`, and an ESC/emergency handle. Do not expose raw HWND.
- Integrate Task 13 UI/library/main composition: saved identity separate from editable draft; bounded events; Validate/Save/Dry Run/Run Once/Run/Pause/Resume/Stop; direct ESC path; library/history/package commands; local image target/region/template re-verification flow.
- Run cumulative review, full automated verification, then ask the user to perform the exact manual gate. Task 14 endurance/corpus work remains pending.
</remaining_work>

<decisions_made>

- Text conditions route to Windows OCR; image conditions route to the lightweight image matcher through a typed detector router.
- Dry Run never masquerades as live Run. Live dispatch happens inline inside runtime action planning through `ActionCommitter`, never by reacting to delayed UI events.
- Only checked immutable saved revisions run; editing a newer draft cannot mutate the active snapshot.
- Continuous/Unlimited removes user-selected bounds only. Cancellation, focus/target checks, pacing, bounded memory/logs/storage, and typed failure handling remain mandatory.
- Portable image verification is always untrusted. Image packages remain nonmutating until exact local target/region/template recapture and local verifier-owned proof complete.
- The native live factory is a separate checkpoint from the live core to keep review scope bounded.
</decisions_made>

<blockers>

- Package commit `546c045` has three unresolved P1 review findings (local target/region authority, forgeable proof inputs, and file growth/reparse races).
- Live commit `8c1aca7` is green but its independent review did not finish before the stop request.
- No honest live UI can be enabled until the exact-HWND Windows factory lands and the live core review is clean.
- Broad `cargo fmt --check` has known unrelated drift in `src/engine/enchant_loop.rs`; use touched-file `rustfmt --edition 2024 --check --config skip_children=true`.
</blockers>

<context>
Continue with strict subagent-driven/TDD review gates. Preserve unrelated work and use the existing worktree. Commit remediations separately and re-run the same independent reviewer until `No findings`. Coordinate the shared git index when parallel agents are active. One unrelated `cargo run --no-default-features` process was left untouched because it is likely the user's separate `C:\infield` app; no Diablo companion executable was running at pause time.
</context>

<next_action>
Start with: verify `git status --short` and HEAD `8c1aca7`, then assign the package implementer a regression-first remediation for the three P1 findings. In parallel, restart a read-only review of `8c1aca7` only if file ownership does not overlap. Resume via `$gsd-resume-work`.
</next_action>
